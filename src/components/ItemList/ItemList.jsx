const ItemList = ({products}) => {
    return(
        <section>
            {
                products.map(product => { //map transforma los productos en un componente card

                })
                }
        </section>
    )
}

export default ItemList;